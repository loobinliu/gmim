services:
  app:
    build:
      context: .
    env_file:
      - .env.docker
    environment:
      NODE_ENV: production
      PORT: "3000"
      HOSTNAME: "0.0.0.0"
    ports:
      - "3000:3000"
    volumes:
      - jitang-data:/app/data
    restart: unless-stopped

volumes:
  jitang-data:
